@import "libc_gate.saasm-layout"
@import "../ffi/libc.saasm-iface"

@ffi_wrapper libc_probe_gate(*probe: ptr) -> i32:
L_ENTRY:
    view = assume_borrow probe
    tag = load view+LibcProbe_tag as i32
    !view
    return tag
