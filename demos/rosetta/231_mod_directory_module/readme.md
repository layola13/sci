# 231 - Mod Directory Module

## 目标特性 (Target Feature)
展示以 `module/index.sa` 为入口的目录模块树。

## 文件结构
- `module/index.sa` 作为目录根。
- `module/tree/index.sa` 再向下展开一层。
- `module/tree/leaf.sa` 保存最底层值。

## 结果
- 编译通过，输出 `231`.
