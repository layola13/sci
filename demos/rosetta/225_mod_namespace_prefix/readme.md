# 225 - Mod Namespace Prefix

## 目标特性 (Target Feature)
展示 `ns/prefix/` 目录与 `ns_prefix_*` 函数名如何一起表达命名空间前缀。

## 文件结构
- `ns/prefix/index.sa` 聚合所有同前缀帮助函数。
- `ns/prefix/alpha.sa` 和 `ns/prefix/beta.sa` 分别提供前缀化条目。

## 结果
- 编译通过，输出 `225`.
