@import "paths/first/index.sa"
@import "paths/second/index.sa"

@export path_value() -> i32:
L_ENTRY:
    first = call @first_path_value()
    second = call @second_path_value()
    value = add first, second
    !first
    !second
    return value
