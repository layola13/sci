@import "bridge/dynamic_gate.sal"
@import "bridge/../ffi/dynamic_lib.sai"

@ffi_wrapper dynamic_probe_gate(*slot: ptr) -> i32:
L_ENTRY:
    view = assume_borrow slot
    tag = load view+DynamicProbe_tag as i32
    !view
    return tag
