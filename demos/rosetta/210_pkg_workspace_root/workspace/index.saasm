@import "members/alpha/index.saasm"
@import "members/beta/index.saasm"

@workspace_total() -> i32:
L_ENTRY:
    alpha = call @alpha_package_value()
    beta = call @beta_package_value()
    value = add alpha, beta
    !alpha
    !beta
    return value
