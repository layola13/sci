@ffi_wrapper boundary(*raw: ptr) -> i32:
L_ENTRY:
    view = assume_borrow raw
    safe = assume_safe raw
    current = load safe+0 as i32
    next = add current, 1
    store safe+0, next as i32
    !current
    !view
    !safe
    return next
