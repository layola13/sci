@import "bin/alpha/index.sa"
@import "bin/beta/index.sa"

@bins_total() -> i32:
L_ENTRY:
    alpha_value = call @alpha_total_value()
    beta_value = call @beta_total_value()
    value = add alpha_value, beta_value
    !alpha_value
    !beta_value
    return value
