@import "src/flags/helpers/flags.sal"

@flags_helper_value() -> i32:
L_ENTRY:
    alpha = add FLAG_ALPHA, 0
    beta = add FLAG_BETA, 0
    gamma = add FLAG_GAMMA, 0
    sum = add alpha, beta
    value = add sum, gamma
    !alpha
    !beta
    !gamma
    !sum
    return value
