@import "cycle/index.saasm"

// 226 - Mod Cyclic Import Detect
// The entry point goes through cycle/index.saasm, which fans into a nested
// core pair that imports itself through same-directory siblings.

@main() -> i32:
L_ENTRY:
    value = call @cycle_value()
    return value
