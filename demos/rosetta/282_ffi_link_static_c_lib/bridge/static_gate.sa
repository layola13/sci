@import "bridge/static_gate.sal"
@import "bridge/../ffi/static_lib.sai"

@ffi_wrapper static_probe_gate(*slot: ptr) -> i32:
L_ENTRY:
    view = assume_borrow slot
    tag = load view+StaticProbe_tag as i32
    !view
    return tag
