# sa_std package manifest
#
# This file intentionally stays empty for now. The standard library is
# sourced from the repo tree directly, and this manifest only marks the
# package boundary for future `sa.pkg` integration.
