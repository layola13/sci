# sa_std package manifest
#
# The compiler-shipped standard library is sourced from this repo tree.
# The package declaration makes the boundary explicit for sa.pkg resolution;
# dependency, version, feature, and ABI-release fields remain unsupported by
# the current manifest grammar and are documented as architecture follow-up.
package "sa_std"